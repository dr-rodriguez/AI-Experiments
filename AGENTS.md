Rules for all coding assistants:
- Always work in the proper subdirectory of the active files.
- Always use `uv run` to run code so you have the proper    environment.
- Prefer simpler solutions over more complex ones.
